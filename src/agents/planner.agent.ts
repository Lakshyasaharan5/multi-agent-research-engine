export async function runPlannerAgent(cleanedQuery: string) {
  return {
    tasks: [
      "Research the topic",
      "Gather relevant information",
      "Analyze the data",
      "Create a summary",
    ],
  };
}